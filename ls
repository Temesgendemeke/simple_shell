#include "main.h"
/*
 * this is a command for list
 */

int main(void)
{
if (strcmp(args[0], "ls") == 0)
{
	while (/bin/ls[i])
	{
		printout(/bin/ls[i]);
		printout("\n");
		i++;
	}
	i = 0;
	continue;
}
}
